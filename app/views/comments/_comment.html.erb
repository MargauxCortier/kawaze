<% comments.each do |c|%> 
  <%= c.content %>

  <%= link_to 'Delete', category_forum_post_comment_path(@category, @forum, post, c),
              method: :delete, class: "button is-danger", data: { confirm: 'Are you sure?' } %>
<% end %>
