<%= form_tag(category_forum_post_comments_path(@category, @forum, post), :method => :post) do  %>

	<%= label_tag :content %>
	<%= text_field_tag :content, nil,class: 'form-control' %>

	<%= submit_tag "Submit", class: "button is-primary" %>
<% end %>