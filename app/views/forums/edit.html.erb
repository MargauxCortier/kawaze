<% content_for :page_title, "Edit Forum" %>
<%= form_tag(category_forum_path(@category, @forum), :method => :put) do  %>
            
  <%= label_tag :title %>
  <%= text_field_tag :title, @forum.title, class: 'form-control' %>

  <%= submit_tag "Submit", class: "button is-primary" %>
<% end %>
