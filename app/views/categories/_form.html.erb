<%= form_tag(@category, :method => :post) do  %>
            
	<%= label_tag :title %>
	<%= text_field_tag :title, @category.title, class: 'form-control' %>

	<%= submit_tag "Submit", class: "button is-primary" %>
<% end %>